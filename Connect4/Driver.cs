namespace Connect4
{
    public static class Driver
    {
        // driver code
        public static void Test()
        {
            //GameState gameState = new();
            //gameState.Drop(1, 0);
            //gameState.Drop(1, 0);
            //gameState.Drop(1, 0);
        }
    }
}
