namespace Connect4
{
    public static class Driver
    {
        // driver code
        public static void Test()
        {
            GameState gameState = new GameState();
            gameState.drop(1, 0);
            gameState.drop(1, 0);
            gameState.drop(1, 0);
        }
    }
}
